{
  "name": "backend-factory",
  "description": "Visualize any backend architecture as an animated retro pixel-art factory. Scans Express.js, Flask, and FastAPI codebases to detect routes, middleware, databases, caches, queues, auth, and more — then renders a localhost web app with animated pixel characters showing request flows.",
  "version": "1.0.0",
  "author": {
    "name": "randyquaye"
  }
}
